export type News = {
  id: string;
  title: string;
  src: string;
  cover: string;
  source: string;
  transcript: string;
  date: string;
};

export type Translation = {
  src: string;
  dst: string;
};
